{
  "name": "discord-interpreter",
  "version": "0.1.3",
  "description": "Native-feeling Discord web wrapper with local private translation overlays.",
  "main": "dist/main/main.js",
  "author": "Local",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rimraf dist release",
    "build": "tsc && node scripts/copy-renderer.mjs",
    "dev": "npm run build && electron .",
    "pack": "npm run build && electron-builder --dir",
    "dist": "npm run build && electron-builder",
    "dist:mac": "npm run build && electron-builder --mac dmg",
    "dist:mac:arm64": "npm run build && electron-builder --mac dmg --arm64",
    "dist:mac:x64": "npm run build && electron-builder --mac dmg --x64",
    "dist:win": "npm run build && electron-builder --win nsis --x64",
    "dist:win:arm64": "npm run build && electron-builder --win nsis --arm64",
    "version:patch": "npm version patch --no-git-tag-version",
    "version:minor": "npm version minor --no-git-tag-version",
    "version:major": "npm version major --no-git-tag-version"
  },
  "dependencies": {
    "electron-updater": "^6.8.3",
    "franc-min": "^6.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "electron": "^41.3.0",
    "electron-builder": "^26.0.12",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  },
  "build": {
    "appId": "local.discord.interpreter",
    "productName": "Discord Interpreter",
    "icon": "build/icon.ico",
    "directories": {
      "output": "release"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "prooshani",
        "repo": "Discord-interpreter"
      }
    ],
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "DI logo-s.png",
        "to": "DI logo-s.png"
      }
    ],
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "category": "public.app-category.productivity",
      "hardenedRuntime": true,
      "gatekeeperAssess": false
    },
    "win": {
      "artifactName": "${productName} Setup ${version}-${arch}.${ext}",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Discord Interpreter",
      "uninstallDisplayName": "${productName} ${version}",
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico",
      "differentialPackage": true,
      "include": "build/installer.nsh"
    }
  }
}
